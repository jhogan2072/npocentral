<h1>Listing accounts</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Full domain</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @accounts.each do |account| %>
  <tr>
    <td><%= account.name %></td>
    <td><%= account.description %></td>
    <td><%= account.full_domain %></td>
    <td><%= link_to 'Show', admin_account_path(account) %></td>
    <td><%= link_to 'Edit', edit_admin_account_path(account) %></td>
    <td><%= link_to 'Destroy', [:admin, account], method: :delete, data: { confirm: t('common.areyousure') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Account', new_admin_account_path %>
