<h1>Listing auctions</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Description</th>
    <th>Event date</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @auctions.each do |auction| %>
  <tr>
    <td><%= auction.name %></td>
    <td><%= auction.description %></td>
    <td><%= auction.event_date %></td>
    <td><%= link_to 'Show', employee_auction_path(auction) %></td>
    <td><%= link_to 'Edit', edit_employee_auction_path(auction) %></td>
    <td><%= link_to 'Destroy', employee_auction_path(auction), method: :delete, data: { confirm: t('common.areyousure') } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Auction', new_employee_auction_path %>
